server:
  port: 8198

spring:
  application:
    name: consul-demo

  cloud:
    consul:
      discovery:
        prefer-ip-address: true
        service-name: ${spring.application.name}
      config:
        format: yaml
